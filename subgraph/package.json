{
  "name": "fumaswap-v4-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for FumaSwap V4 on Fushuma Network",
  "repository": {
    "type": "git",
    "url": "https://github.com/Fushuma/fushuma-gov-hub-v2"
  },
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ fumaswap-v4",
    "remove-local": "graph remove --node http://localhost:8020/ fumaswap-v4",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fumaswap-v4",
    "deploy": "graph deploy --studio fumaswap-v4",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.5",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
